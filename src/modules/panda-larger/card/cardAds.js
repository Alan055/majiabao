let prefix_common = 'DW_clickH400_2_1';
let prefix_index = prefix_common + '1';

let index = {
  clickMessage: prefix_index + '01', // 点击“消息”
  clickKefu: prefix_index + '02', // 点击“客服”
};

export default {
  index
};
